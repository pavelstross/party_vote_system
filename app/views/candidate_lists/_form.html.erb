<%= form_for(candidate_list) do |f| %>
  <% if candidate_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(candidate_list.errors.count, "error") %> prohibited this candidate_list from being saved:</h2>

      <ul>
      <% candidate_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
